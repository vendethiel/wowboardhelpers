$types = {show: green, hide: red}
for $type, $color in $types
	a.{$type}-topic
		cursor: pointer
		float: left
		margin-top: 13px
		color: $color

		&:hover
			color: $color !important

// display:none moves things a bit ...
.last-read
	opacity: 0

tr:hover .last-read
	opacity: 1